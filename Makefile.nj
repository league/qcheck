# -*- Makefile -*-
default: src/qcheck-ver.sml
	echo 'CM.stabilize true "qcheck.cm";' | $(SML)
all: default
test: default
	$(SML) -m tests/tests.cm </dev/null

mostlyclean:
	$(RM) -r {src,tests,compat/nj}/$(CM)

clean: mostlyclean
	$(RM) -r $(CM)

realclean: clean

include Makefile.version
